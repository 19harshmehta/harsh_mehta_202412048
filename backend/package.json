{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "jest",
    "prisma:generate": "prisma generate --schema prisma/sql.prisma && prisma generate --schema prisma/mongo.prisma",
    "build": "npm run prisma:generate && prisma migrate deploy --schema prisma/sql.prisma && prisma db push --schema prisma/mongo.prisma && node seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.7.0",
    "prisma": "^5.22.0"
  }
}
